import CustomizeServiceList from '@/app/_components/CustomizeServiceList/CustomizeServiceList';
import React from 'react';

const page = () => {
    return (
        <>
            <CustomizeServiceList/>
        </>
    );
};

export default page;
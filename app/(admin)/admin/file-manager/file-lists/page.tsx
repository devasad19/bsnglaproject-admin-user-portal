import FileList from '@/app/_components/FileList/FileList';
import React from 'react';

const page = () => {
    return (
        <>
            <FileList/>
        </>
    );
};

export default page;